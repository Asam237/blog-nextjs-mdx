export type SolutionType = {
  title: string;
  content: string;
};

export type PublicationType = {
  id: string;
  zone: string;
  date: string;
  title: string;
  header: string;
  content: string;
  image?: any;
};
